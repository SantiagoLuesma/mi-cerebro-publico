# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# github
- When the user asks about their GitHub repos, first use `mcp__github__get_me` to identify the authenticated user, then search within that user's repos using `mcp__github__search_repositories` with the query scoped to the user. Confidence: 0.65

